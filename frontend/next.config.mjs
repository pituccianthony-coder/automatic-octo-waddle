/** @type {import('next').NextConfig} */
const nextConfig = {
    // We can add configurations here later if needed,
    // like redirects, rewrites, or image optimization domains.
    // For now, the default configuration is divine enough.
};

export default nextConfig;
